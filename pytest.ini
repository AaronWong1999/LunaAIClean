[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    telethon: marks tests as Telethon integration tests (deselect with '-m "not telethon"')
    slow: marks tests as slow running
